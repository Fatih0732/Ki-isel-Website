/* Genel Sayfa Ayarları */
body {
    font-family: Arial, sans-serif; /* Yazı tipi */
    margin: 0;
    padding: 0;
    background-color: #f4f4f4; /* Hafif gri arka plan */
}

/* Header (Üst Başlık) Alanı */
header {
    background-color: #FF4500; /* Antalya'nın meşhur portakal rengi :) */
    color: white;
    padding: 20px;
    text-align: center;
}

/* Navigasyon (Menü) */
nav {
    background-color: #333;
    padding: 10px;
}

nav ul {
    list-style-type: none; /* Madde işaretlerini kaldır */
    padding: 0;
    text-align: center;
}

nav ul li {
    display: inline; /* Menüleri yan yana getir */
    margin-right: 20px;
}

nav ul li a {
    color: white;
    text-decoration: none; /* Alt çizgiyi kaldır */
    font-weight: bold;
}

nav ul li a:hover {
    color: #FF4500; /* Üzerine gelince portakal rengi olsun */
}

/* Ana İçerik Alanı */
main {
    padding: 20px;
}

/* Footer (Alt Bilgi) */
footer {
    text-align: center;
    padding: 10px;
    background-color: #333;
    color: white;
    position: fixed; 
    bottom: 0;
    width: 100%;
}
